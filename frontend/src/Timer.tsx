// Timer component
 

const Timer = () => {
  return (
    <div style={{ padding: '8px', display: 'grid', justifyItems: 'center' }}>
      <img src="/assets/timer.gif" alt="timer" style={{ border: '2px solid #c9c9c9' }} />
    </div>
  );
};

export default Timer;
